# 戦国シミュレーション　Claude Code への移行手順

このフォルダの中身と、移行のやり方をまとめました。
専門の知識がなくても進められるよう書いています。

---

## 一、このフォルダに入っているもの

| ファイル | 中身 |
|---|---|
| `sengoku-prototype.jsx` | **本体**。11,715行。これがゲームのすべてです |
| `harness.jsx` | 試験用の包み。本体を機械的に動かすために使います |
| `run5.cjs` | 政務の試験（24か月ぶん自動で進める） |
| `run8.cjs` | すべての命令を一通り試す |
| `run9.cjs` | 合戦の試験 |
| `save.cjs` | セーブと読み込みの試験 |
| `sel.cjs` | 大名を選ぶ画面の試験 |
| `succ.cjs` | 家督相続・調略・外交の試験 |
| `watch.cjs` | 見物モードで長期に走らせる |
| `ct.cjs` | 城攻めの試験 |

**本体さえあれば動きます。** 試験は「直したときに壊していないか確かめる」ためのものです。

---

## 二、Claude Code に移す手順

### 1. フォルダを作る

パソコンのどこかに `sengoku` というフォルダを作り、このフォルダの中身をすべて入れます。

### 2. Claude Code を開く

そのフォルダで Claude Code を開きます。

### 3. 最初にこう伝える

```
このフォルダの sengoku-prototype.jsx は、私が作っている戦国シミュレーションです。
11,715行の単一ファイルで、Claude の Artifacts で動く React コンポーネントです。

まず以下をお願いします。
1. ファイルを読んで、全体の構造を把握してください
2. handoff フォルダの引き継ぎ資料（下記）を読んでください
3. ファイルを機能ごとに分割する計画を立ててください
```

### 4. 引き継ぎ資料を渡す

同じフォルダに置いてある **`引き継ぎ資料.md`** を読ませてください。
これまでの経緯、実装済みの仕様、未解決の課題がすべて書いてあります。

---

## 三、Code で最初にやるべきこと（順番）

### 第一段階・ファイルを分割する（半日ほど）

11,715行が一つのファイルに入っているのが、いちばんの問題です。
これを次のように分けます。

```
src/
  data/       城249・武将812・街道426・勢力113のデータ
  govern/     政務（内政・外交・調略・検地）
  battle/     合戦（野戦・城攻め）
  ui/         画面
  save/       セーブと読み込み
```

**分割するだけで、直すときの事故が激減します。**

### 第二段階・試験を自動で走らせる（数時間）

いまある `run5.cjs` などを、コマンド一つで全部走らせられるようにします。
これで「直したら壊れた」がすぐ分かります。

### 第三段階・ブラウザ版を作る（一日ほど）

一つの HTML ファイルとして書き出せば、**開くだけで遊べます。**
セーブはブラウザに記憶されるので、ファイルを気にする必要がありません。

### 第四段階・アプリ化（あとで）

ブラウザ版ができれば、スマホのアプリにするのは比較的容易です。
急ぐ必要はありません。

---

## 四、Code に伝えると良いこと

移行後、こう伝えると作業が進みやすいです。

```
・このゲームの方針は「見た目はシンプルに、内容はできるだけ史実に近く」です
・日本語で応答してください
・コードの注釈も日本語で、当時の言葉づかいを意識してください
  （例：「兵を退く」「門を破る」「家督を継ぐ」）
・修正のたびに試験を走らせて、壊していないか確かめてください
```

---

## 五、注意していただきたいこと

**このファイルは大きいので、Code でも一度に全部は読めません。**
「まず構造を把握して、分割の計画を立てて」と伝えるのが確実です。

**分割の途中で動かなくなることがあります。**
その場合は、いまのファイルをそのまま残しておけば戻せます。
**必ず元のファイルの控えを取ってから始めてください。**
