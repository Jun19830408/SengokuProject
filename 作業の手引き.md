# 戦国プロジェクト　作業の手引き

分割後の folder の見取り図と、日々の使い方をまとめました。
専門の知識がなくても進められるよう書いています。

---

## 一、まず覚える三つの言葉

folder を開いた画面（端末・ターミナル）で打ちます。

| 打つこと | 何が起きるか |
|---|---|
| `npm start` | **携帯で遊ぶための宿を立てます。** 打つと宛先が出ます |
| `npm test` | **すべての試験が走ります。** 末尾に「不首尾 0 件」と出れば無事です |
| `npm run build` | **遊ぶ形に書き出します。** `dist/戦国.html` と `dist/sengoku-artifact.jsx` |
| `npm run compare` | **原本と見比べます。** 直したせいで動きが変わっていないかを確かめます |

**直したら `npm test`。** これだけ守れば、壊したまま気づかない、ということは起きません。

---

## 二、folder の中身

```
sengoku/
  src/                 ここが本体。40の折に分かれています
  dist/                書き出し先（遊ぶのはこちら）
  tests/               試験
  tools/               書き出しと確かめの道具
  sengoku-prototype_1.jsx   分割前の原本（控え。触りません）
```

### src の中

| 折 | 中身 |
|---|---|
| `data/` | 盤の素材。城249・武将812・街道426・勢力113、海岸線と国境 |
| `core/` | 決まりごと。身分と禄高、家督、経路、検地、官位、寿命、海戦 |
| `govern/` | 政務。**`month.js` が月送り**。ほかに命令・外交・合戦の裁定・家の方針 |
| `battle/` | 合戦。野の地形、城郭図、陣形、進行、合戦AI、描画 |
| `ui/` | 画面。題名、大名選び、政略図、城の帳面、合戦画面 |
| `save/` | 記録。`store.js` が置き場、`save.js` が包み方 |

**参照の向きは `data` → `core` → `govern`／`battle` → `ui` の一方通行です。**
逆向きに呼ぶと、思わぬところが壊れます。

---

## 三、遊ぶ形

`npm run build` で二つ出ます。

| 書き出し | 使い方 |
|---|---|
| `dist/戦国.html` | **これ一つを開けば遊べます。** 他のファイルは要りません |
| `dist/index.html` | 中身は上とまったく同じ。**ネットに置くならこちら** |
| `dist/sengoku-artifact.jsx` | Artifacts に貼る形 |

`index.html` を別に出しているのは、ネットに公開したとき
**URLの末尾に何も付けずに開ける**のがこの名前だけだからです。
「戦国.html」は日本語なので、URLにすると `%E6%88%A6%E5%9B%BD.html` という
長い文字列になってしまい、手で打つのは無理があります。

### 携帯で遊ぶには（`npm start`）

1. パソコンと iPhone を**同じ Wi-Fi**に繋ぐ
2. パソコンで `npm start` と打つ
3. 出てきた `http://192.168.〇.〇:8080` を、iPhone の Safari に打ち込む

**なぜ宿を立てるのか。** `dist/戦国.html` を iPhone へ送って「ファイル」から直に開くと、
iPhone では**記録が残らぬことがあります**（file:// で開いた頁には記憶を許さぬ仕組みのため）。
宿を立てて `http://` で開けば、きちんと残ります。

直したときは、パソコンで `npm run build` を打ち、iPhone の画面を下へ引くだけで新しくなります。

### ホーム画面に追加すると

Safari の共有ボタン → **「ホーム画面に追加」**。
住所欄のない一枚の画面として開きます。**将来アプリにする形に近いのはこちらです。**

ただし中身はパソコンから配られているので、**パソコンで `npm start` を止めると開けません。**

**記録はその端末のブラウザに残ります。** 別の端末へは自動では移りません。

### 外出先からも遊ぶには（ネットに公開する）

`npm start` は同じ Wi-Fi の中だけです。パソコンを止めると開けなくなります。
外出先の携帯回線からも遊ぶなら、`dist` フォルダを**無料のホスティングサービス**に置きます。

このゲームは**ただのHTML1枚**なので、置くだけで動きます。
サーバーの設定も、データベースも、月額料金も要りません。

**Netlify（ネットリファイ）が一番かんたんです。**

1. `netlify.com` を開き、**Sign up** でアカウントを作る
   （メールアドレスとパスワードだけ。クレジットカードは不要）
2. `app.netlify.com/drop` を開き、**`dist` フォルダをマウスで掴んで画面に放り込む**
3. 10秒ほどで `https://〇〇〇.netlify.app` というURLが出る。iPhone でそれを開く

**上げたら、鍵がかかっていないか確かめてください。**
Netlify は新しいサイトに鍵をかけることがあります。かかっていると、
Netlify にログインした人しか開けません（自分のパソコンでは開けるので気づきにくい）。

*確かめ方* ── ブラウザの**シークレットウィンドウ**でURLを開く。
ログイン画面が出たら、鍵がかかっています。

*外し方* ── サイトの設定 → **Access & security**（アクセスと安全）→
**Visitor access**（訪問者のアクセス）を **Public**（公開）にする。
「Password protection」が入っていれば、それも外します。

**放り込んだら、真っ先にURLの名前を変えてください。**
サイトの設定（Site configuration → Change site name）で、
`matsui-sengoku` のように入れると `https://matsui-sengoku.netlify.app` になります。

なぜ真っ先かというと、**セーブデータはURLごとに別々に保存されるから**です。
遊び込んだ後でURLを変えると、それまでのセーブが前のURLに置き去りになります。
（そうなったら「控えを書き出す」で取り出し、新しいURLで「控えから戻す」）

**直したときの更新のしかた**

1. パソコンで `npm run build`
2. Netlify のそのサイトの **Deploys** のページを開く
3. そこにも同じ放り込み口があるので、**`dist` フォルダをまた放り込む**

URLは変わりません。セーブも消えません。

| | `npm start` | ネットに公開 |
|---|---|---|
| 外出先の携帯回線から | ✗ | ○ |
| パソコンを止めても | ✗ | ○ |
| 更新のしかた | `npm run build` | `npm run build` ＋ フォルダを放り込む |
| 料金 | ― | 無料の範囲で足ります |

**URLを知っている人は誰でも開けます。** デタラメな文字列なので偶然たどり着く人はいませんが、
パスワードで守る仕組みではありません。人に見られて困る中身ではないはずです。

Netlify でなくても構いません。GitHub Pages・Cloudflare Pages でも同じことができます。
**`dist` の中身を置ければ、どこでも動きます。**

---

## 四、記録について

置き場は `src/save/store.js` が決めます。いまは二つ。

- **Artifacts の蔵**（`window.storage`）── Artifacts で遊ぶとき
- **ブラウザの記憶**（`localStorage`）── ブラウザで開いて遊ぶとき

後日、端末の間で記録を分け合いたくなったら、**`store.js` に置き場を一つ足すだけ**です。
`読む／書く／消す` の三つさえ備えれば、呼ぶ側に手を入れる必要はありません。

### 履歴の削除で消える件

**ブラウザの「履歴と Web サイトデータを消去」を行うと、記録も消えます。**
これを web ページの側から防ぐ手立てはありません。

そのため、題名の画面に**「控えを書き出す」**を置きました。
`戦国_1553年03月_織田家.json` のような名で手元に落ちます。
iPhone なら「ファイル」に収まり、iCloud を通じて別の端末へも移せます。
戻すときは**「控えから戻す」**です。

---

## 五、直すときの順序

1. `src/` の中の、直したい折を開く
2. 直す
3. `npm test` を走らせる（末尾が「不首尾 0 件」なら無事）
4. 遊びの中身に触れたなら `npm run compare` も走らせる
5. `npm run build` で書き出す

### 取り込み（import）が足りないと言われたら

`node tools/imports.cjs` を打つと、**足りない取り込みを自動で足します。**
折をまたいで処理を動かしたときは、これを一度打てば済みます。

---

## 六、調べるための道具

| 打つこと | 分かること |
|---|---|
| `node tools/bench.cjs 120` | **月送りの重さ。** 画面を描かずに10年ぶん回します |
| `node tools/bench-battle.cjs` | 合戦一戦の重さ |
| `node tools/bench-battle-dt.cjs` | 合戦の刻みを変えたときの、勝敗の食い違い |
| `npm run compare -- 240` | 原本との突き合わせ（月数を指定できます） |

**`bench.cjs` が「50年で天下が定まらぬ」の検証の足がかりです。**
月送りを画面から切り離したので、絵を描かずに何百年でも回せます。

---

## 七、いま分かっている課題

- **天下が定まらない**（引き継ぎ資料の「四、未解決の課題」）。まだ手をつけていません
- **同じ家どうしが戦う**ことがある。長期走行の記録に
  「伊賀上野下で六角家と六角家が戦い」という一行が出ました。
  城が寝返った後も、軍が向かい続けているのだと思われます
- **合戦の省略は、手ずから戦った場合と一割ほど勝敗が分かれます。**
  同じ仕組み・同じ布陣で刻みだけを変えて20戦させたところ、18戦は同じ結果でした。
  残る2戦は、もともと紙一重の戦でした

---

## 八、ネットに置く（いまの形と、公開のしかた）

### いまの形

```
私が直す → git push（私） → GitHub → Netlify が自動で組み立て → 同じ住所で最新
```

| | |
|---|---|
| 遊ぶ住所 | `https://sengokuproject.netlify.app` |
| 置き場 | `https://github.com/Jun19830408/SengokuProject`（**非公開**） |
| 鍵 | かかっている。開くには Netlify へのログインが要る |

**私が「送りました」と申し上げた一分後には、画面を下に引くだけで最新になります。**

### 鍵がかかっている理由

**置き場を非公開にしているため**と思われます。Netlify は、非公開の置き場から作ったサイトを
保護することがあります。iPhone で開くと Netlify のログイン画面へ回されるのは、これです。

ログインは「Continue with GitHub」を選ぶのが確実です。

### 公開にする手順（鍵を外したくなったら）

1. `https://github.com/Jun19830408/SengokuProject` を開く
2. 上の並びの **Settings**（歯車の絵）
3. 一番下までさがると **Danger Zone**（赤い枠）
4. **Change repository visibility** → **Change to public**
5. 置き場の名前 `Jun19830408/SengokuProject` を打ち込んで確かめる
6. **I understand, change repository visibility**

**数分のうちに Netlify の鍵も外れる見込みです。** 外れなければ、Netlify 側の
**Site configuration → Access & security** で保護を解いてください。

### 公開にすると、何が見えるようになるか

**見えるもの**

- ゲームの設計図いっさい（`src` の41の折、試験、道具、原本）
- これまでの直しの記録と、そこに添えた説明
- **記録を書いた者のメールアドレス**（`matsui.a.link@gmail.com`）
  ── これは公開されます。避けたい場合は、GitHub の設定で
  `Settings → Emails → Keep my email addresses private` を入れてから公開してください

**見えないもの**

- 遊んだ記録（セーブ）── ブラウザの中にあり、置き場には入りません
- 合言葉や鍵の類 ── 収めていないことを確かめてあります
- SSH の秘密の鍵 ── Mac の中（`~/.ssh`）にあり、置き場には入りません

### 公開にすると、できるようになること

- **Netlify の鍵が外れ、住所を打つだけで遊べます**（ログイン不要）
- **GitHub Pages** という別の置き場も無料で使えます
  （`https://jun19830408.github.io/SengokuProject/`）。Netlify が不調のときの控えになります

### 元に戻すには

同じ **Danger Zone** から **Change to private** で戻せます。
ただし、**一度公開したものは誰かが写している可能性があります。** 戻しても、それは消えません。
